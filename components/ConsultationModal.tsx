"use client";
import { useEffect, useState } from "react";
export default function ConsultationModal() { const [open, setOpen] = useState(false); const [sent, setSent] = useState(false); const [busy, setBusy] = useState(false); const [error, setError] = useState(""); useEffect(() => { const handler = () => setOpen(true); window.addEventListener("open-consultation", handler); return () => window.removeEventListener("open-consultation", handler); }, []); if (!open) return null; async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); setBusy(true); setError(""); const data = Object.fromEntries(new FormData(e.currentTarget)); const response = await fetch("/api/appointments", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }); setBusy(false); if (response.ok) setSent(true); else setError("We could not send your request right now. Please try again or contact our care team."); } return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/65 p-4 backdrop-blur-sm" role="dialog" aria-modal="true" aria-labelledby="consultation-title"><div className="relative w-full max-w-lg rounded-3xl bg-white p-7 shadow-2xl sm:p-9"><button onClick={() => setOpen(false)} className="absolute right-5 top-4 text-2xl text-slate-500" aria-label="Close">×</button>{sent ? <div className="py-8 text-center"><div className="mx-auto grid size-14 place-items-center rounded-full bg-teal-100 text-2xl text-teal-800">✓</div><h2 className="mt-5 font-display text-2xl font-bold">Thank you for reaching out.</h2><p className="mt-3 text-sm leading-6 text-slate-600">Your consultation request has been received. A coordinator will review it and respond through your preferred contact details.</p><button onClick={() => setOpen(false)} className="mt-7 rounded-full bg-teal-700 px-5 py-3 text-sm font-bold text-white">Close</button></div> : <><p className="text-xs font-bold uppercase tracking-[.16em] text-teal-700">Private case consultation</p><h2 id="consultation-title" className="mt-2 font-display text-3xl font-bold text-slate-950">Let’s talk about your care.</h2><p className="mt-3 text-sm leading-6 text-slate-600">Please share only the basics here. Do not include medical records or sensitive health details in this form.</p><form onSubmit={submit} className="mt-6 grid gap-4"><label className="grid gap-1 text-sm font-semibold">Full name<input required name="name" className="rounded-xl border border-slate-300 px-3 py-3 font-normal" /></label><div className="grid gap-4 sm:grid-cols-2"><label className="grid gap-1 text-sm font-semibold">Email<input required name="email" type="email" className="rounded-xl border border-slate-300 px-3 py-3 font-normal" /></label><label className="grid gap-1 text-sm font-semibold">Phone<input required name="phone" type="tel" className="rounded-xl border border-slate-300 px-3 py-3 font-normal" /></label></div><label className="grid gap-1 text-sm font-semibold">Treatment area<select name="service" className="rounded-xl border border-slate-300 px-3 py-3 font-normal"><option>General consultation</option><option>Cardiology</option><option>Oncology</option><option>Organ transplantation</option><option>Other specialty</option></select></label><label className="flex items-start gap-2 text-xs leading-5 text-slate-600"><input required name="consent" type="checkbox" className="mt-1" />I consent to Shifa Global Care contacting me about this consultation request.</label>{error && <p className="text-sm text-red-700">{error}</p>}<button disabled={busy} className="rounded-full bg-teal-700 px-5 py-3.5 text-sm font-bold text-white disabled:opacity-60">{busy ? "Sending…" : "Request consultation"}</button></form></>}</div></div>; }
